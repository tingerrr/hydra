name: Tests
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  tests:
    uses: tingerrr/typst-test/.github/workflows/setup-B.yml@master
    steps:
      - run: |
          just gen
          just test
