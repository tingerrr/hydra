name: Tests
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  setup:
    uses: tingerrr/typst-test/.github/workflows/setup.yml@test-ci
  tests:
    steps:
      - run: |
          just gen
          just test
