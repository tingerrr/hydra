name: Tests
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  setup:
    uses: tingerrr/hydra/.github/workflows/setup.yml@test-ci
  tests:
    runs-on: ubuntu-latest
    steps:
      - run: |
          just gen
          just test
